// Ejercicio 9: Crear variable con la sentencia var

// Declaramos la variable numero
// El tipo de datos de una variable puede variar.
// Se puede declarar varias variables en la misma linea.

var numero;

// asignamos un valor
numero = 7;

// mostramos por consola el valor de la variable
console.log(numero);

// ver el typo de datos de la variable
console.log(typeof(numero));

// asignamos un valor string
numero = '7';

// ver el typo de datos de la variable
console.log(typeof(numero));

var x, y;

var a = 1, b = 'JavaScript';

